# v0.23.0-2 Physical Qualification Ledger

Status: **reserved / pending reference iPhone availability**

This is the authoritative continuation point when the maintainer returns to the
reference iPhone. Local tests, Simulator receipts and a GitHub draft are not
physical evidence.

## Fixed candidate

- package: `com.arthur.roottools` / `0.23.0-2` / `iphoneos-arm64`
- credential profile: `candidate-v0.23.0-2`
- artifact source: private `arthurwangwsx-code/RootTools-runtime-releases`
- exact source commit/tree and asset hashes: read from
  `SOURCE_BUILD_MANIFEST.json` plus `SHA256SUMS`
- rollback artifact: retained `roottools_0.23.0-1_iphoneos-arm64.deb`
- old credential profile: reserved `installed`

Do not edit the four credential files, delete the rollback DEB, or retire the
legacy checkout during this run.

## Gate 0 — identify and preflight

- [ ] Discover the connected iPhone; do not hard-code an old UDID.
- [ ] Confirm the device is unlocked, trusted, jailbroken and `/var/jb` is active.
- [ ] Record the pre-install `/v1/hello` and `/v1/status` using `installed`.
- [ ] Verify downloaded assets against `SHA256SUMS` in a new directory.
- [ ] Confirm the offline migration state says `offline-prepared` and matches the
      downloaded DEB/IPA plus retained rollback DEB.

Record device, time, installed version and evidence path here after the run:

```text
device:
startedAt:
installedDaemonVersion:
installedPackageVersion:
downloadReceipt:
```

## Gate 1 — install and full USB regression

```bash
python3 Scripts/verify-device.py \
  --credential-profile candidate-v0.23.0-2 \
  --install \
  --full
```

- [ ] `/v1/status`: `daemonVersion=0.23.0`.
- [ ] `/v1/status`: `packageVersion=0.23.0-2`.
- [ ] daemon UID is 0 and rootless readiness is true.
- [ ] foreground App is registered, launches as mobile and daemon survives exit.
- [ ] complete typed observation/mutation/post-condition/audit regression passes.

## Gate 2 — credential cutover

```bash
python3 Scripts/verify-credential-transition.py \
  --active-profile candidate-v0.23.0-2 \
  --retired-profile installed
```

- [ ] candidate Owner accepted as Owner.
- [ ] candidate Agent accepted as Agent.
- [ ] installed Owner rejected.
- [ ] installed Agent rejected.
- [ ] mode-`0600` migration state advances to
      `physical-credential-transition-verified` without raw credentials.

## Gate 3 — genuinely off-USB Tailnet

Use `candidate-v0.23.0-2` for every phase in
`remote-access-physical-runbook.md`.

- [ ] USB prepare passes with exact package identity.
- [ ] USB is physically disconnected before remote verification.
- [ ] selected Host R0/R1 succeeds over Tailnet.
- [ ] wrong Host, Owner and Agent credentials are rejected remotely.
- [ ] explicit stop closes authority.
- [ ] expiry closes authority.
- [ ] Principal revoke closes authority and temporary Principals are cleaned up.

## Gate 4 — rollback and recovery decision

Choose and record exactly one outcome:

- [ ] Rehearse rollback to the retained installed-profile DEB, verify old
      Owner/Agent recovery and foreground App registration, then reinstall and
      reverify v0.23.0-2; or
- [ ] record an explicit rollback waiver with reason, risk owner and retained
      recovery artifacts.

```text
rollbackOutcome:
reason:
recoveryEvidence:
```

## Gate 5 — closeout

- [ ] migration, USB and Tailnet evidence all refer to the same source manifest
      and package revision.
- [ ] release is promoted from draft only after Gates 0–4 pass.
- [ ] candidate credentials become the new installed baseline only after
      successful cutover; do not silently overwrite evidence profiles.
- [ ] legacy snapshot retirement is a separate, explicitly authorized cleanup.

```text
completedAt:
acceptedBy:
releaseState:
remainingWaivers:
```
